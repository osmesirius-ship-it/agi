#include <iostream>

int main() {
  std::cout << "oracle runtime target is configured and buildable\n";
  std::cout << "next: wire simulation pipeline into src/ and link shared core libs\n";
  return 0;
}
